'use client'
export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  /**
   * Renders the root layout component.
   *
   * @param {Object} props - The component props.
   * @param {React.ReactNode} props.children - The child components to render..
   *
   * @returns {React.ReactNode} The rendered child components.
   */

  /**
   * Rend le composant de mise en page racine.
   *
   * @param {Object} props - Les propriétés du composant.
   * @param {React.ReactNode} props.children - Les composants enfants à rendre.
   *
   * @returns {React.ReactNode} Les composants enfants rendus.
   */

  return <>{children}</>;
}
